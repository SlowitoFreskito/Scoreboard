package dev.slow.core.scoreboard.impl;

public enum BoardFormat {
	
    SECONDS("SECONDS", 0), 
    MINUTES("MINUTES", 1), 
    HOURS("HOURS", 2);
    
    private BoardFormat(String s, int n) {
    }
}